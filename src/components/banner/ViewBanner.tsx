import { Box } from '@mui/material'

interface IProps {
  url: string
  sizes: string
  quality: number
}
const ViewBanner = (props: IProps) => {
  const { url, sizes, quality } = props
  return <Box></Box>
}

export default ViewBanner
